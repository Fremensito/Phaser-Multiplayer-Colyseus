export interface IEnemy{
    speed: number;
    x:number;
    y:number;
    pointToMoveX: number;
    pointToMoveY: number;
    directionX: number;
    directionY: number;
    health: number
    id: string;
}